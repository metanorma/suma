(*
   Synthetic test schema for the suma dummy-collection fixture.
   Invented content; no ISO 10303 material.
*)
SCHEMA gadget_schema;

TYPE gadget_name = STRING;
END_TYPE;

TYPE gadget_grade = ENUMERATION OF
  (standard,
   premium,
   experimental);
END_TYPE;

ENTITY gadget;
  name    : gadget_name;
  grade   : gadget_grade;
  weight  : REAL;
WHERE
  positive_weight : weight > 0.0;
END_ENTITY;

END_SCHEMA;
